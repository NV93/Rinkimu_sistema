var React = require('react');
var PropTypes = React.PropTypes;

var  CheckBoxDistrictNameComponent = React.createClass({

  render: function() {
    return (
      <div>
        <option>{this.props.}</option>
      </div>
    );
  }

});

module.CheckBoxDistrictNameComponent = CheckBoxDistrictNameComponent;
